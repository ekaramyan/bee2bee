import background from '@/assets/img/auth_bg.svg'
import { Typography } from '@mui/material'

export default function Login() {
	return (
		<div style={{ background: `url(${background.src})` }}>
			<Typography variant=''>Members Login</Typography>
			<>
				<form action=''></form>
			</>
		</div>
	)
}
